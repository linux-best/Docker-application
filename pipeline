pipeline {
    agent any

    environment {
        VAR_DOCKER = 'linuxbest531/python-application'
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/linux-best/Docker-application/'
            }
        }
        stage('Build') {
            steps {
                sh "pwd && cd python-application-1/"
                script {
                    dockerImage = docker.build("${env.VAR_DOCKER}:${env.BUILD_NUMBER}")
                }
                // sh "sudo docker build -t linux531/python-application . -f Dockerfile"
            }
        }
        stage('Test') {
            steps {
                sh 'docker run --rm "${env.VAR_DOCKER}:${env.BUILD_NUMBER}"'
            }
        }
        stage('Push') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', 'dockerhub-credentials') {
                        dockerImage.push()
                    }
                }
            }
        }
    }
}
